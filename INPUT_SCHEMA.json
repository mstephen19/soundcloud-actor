{
    "title": "SoundCloud Scraper",
    "description": "Scrape data from SoundCloud!",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "usernames": {
            "title": "Usernames",
            "type": "array",
            "description": "List of users you want to scrape.",
            "editor": "stringList",
            "maxItems": 50,
            "uniqueItems": true,
            "nullable": false,
            "default": []
        },
        "keywords": {
            "title": "Keywords",
            "type": "array",
            "description": "List of query terms you want to search by, and then scrape the tracks.",
            "editor": "stringList",
            "maxItems": 50,
            "uniqueItems": true,
            "nullable": false,
            "default": []
        },
        "urls": {
            "title": "URLs",
            "type": "array",
            "description": "List of valid SoundCloud URLs either to a user page, or a query page.",
            "editor": "stringList",
            "maxItems": 50,
            "uniqueItems": true,
            "nullable": false,
            "default": []
        },
        "maxComments": {
            "title": "Maximum Comments (0 -> No Comments)",
            "type": "integer",
            "description": "How many comments on each track do you want to scrape? (Max 200)",
            "editor": "number",
            "default": 0,
            "maximum": 250,
            "minimum": 0,
            "unit": "comments"
        },
        "maxQueryResults": {
            "title": "Maximum Query Results (Ignore if not scraping by keywords)",
            "type": "integer",
            "description": "How many results do you want for each keyword search? (Max 1400)",
            "editor": "number",
            "default": 200,
            "maximum": 1400,
            "minimum": 0,
            "unit": "results"
        },
        "clientId": {
            "title": "client_id",
            "type": "string",
            "description": "SoundCloud API Client ID (Leave empty to use default)",
            "editor": "textfield"
        },
        "maxConcurrency": {
            "title": "maxConcurrency",
            "type": "integer",
            "description": "Maximum number of operation that can happen simultaneously.",
            "editor": "number",
            "default": 100,
            "maximum": 999,
            "minimum": 1,
            "unit": "operations"
        },
        "debug": {
            "title": "Debug mode",
            "type": "boolean",
            "default": false,
            "description": "Log to console",
            "sectionCaption": "Features and flags"
        }
    },
    "required": []
}
